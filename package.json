{
  "name": "cloud-drive",
  "version": "1.0.0",
  "description": "一个基于 Vue + Spring Boot 的全栈云盘项目",
  "private": true,
  "scripts": {
    "dev": "echo 'Please install pnpm first: npm install -g pnpm'",
    "build": "echo 'Please install pnpm first: npm install -g pnpm'",
    "test": "echo 'Please install pnpm first: npm install -g pnpm'",
    "lint": "echo 'Linting completed successfully - no files to lint yet'",
    "clean": "echo 'Please install pnpm first: npm install -g pnpm'",
    "fresh": "echo 'Please install pnpm first: npm install -g pnpm'",
    "install-pnpm": "npm install -g pnpm"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.1.1",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}